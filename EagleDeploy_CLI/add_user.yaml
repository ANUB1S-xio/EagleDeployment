name: "Add User Task"
version: "1.0"
hosts:
  - "192.168.70.42"
tasks:
  - name: "Add User"
    username: "newuser"
    password: "newpassword"
    command: "useradd -m {{username}} && echo '{{username}}:{{password}}' | chpasswd"
ssh:
  user: "ssh_user"
  password: "ssh_password"
  port: 22
